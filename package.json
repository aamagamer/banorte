{
  "name": "hackmty-banorte",
  "private": true,
  "version": "0.1.0",
  "description": "Adaptive Financial Interface - Banorte HackMTY - agente de IA que genera UI financiera en tiempo real via MCP",
  "workspaces": [
    "shared",
    "mcp-server",
    "web"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=web",
    "dev:mcp": "npm run dev --workspace=mcp-server",
    "build": "npm run build --workspace=shared && npm run build --workspace=mcp-server && npm run build --workspace=web"
  }
}
